// Bucket + CloudFront defaults for the S3 Navigate UI.
// Change these in one place; the UI sends bucket per call so app-service stays
// generic.

export const BUCKET = 'emergent-frontend-assets';
export const CLOUDFRONT_URL = 'https://assets.emergent.sh';
export const CLOUDFRONT_DISTRIBUTION_ID = 'EKFF4XNB45RIZ';
